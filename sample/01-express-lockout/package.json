{
  "name": "authlock-express-sample",
  "version": "0.0.0",
  "private": true,
  "description": "Bare Express app using ONLY @authlock/core (no NestJS, no DI) — the framework-neutrality proof: a login route locks out after repeated failures and returns a Retry-After.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "smoke": "ts-node --transpile-only scripts/smoke.ts",
    "test": "npm run typecheck && npm run smoke"
  },
  "dependencies": {
    "@authlock/core": "0.4.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^26.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
